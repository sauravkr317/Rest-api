let Joi,REFRESH_SECRET,refresh_token,User,CustomerrorHandler,JwtServices;_a80‍.x([["default",()=>_a80‍.o]]);_a80‍.w("joi",[["default",["Joi"],function(v){Joi=v}]]);_a80‍.w("../../config",[["REFRESH_SECRET",["REFRESH_SECRET"],function(v){REFRESH_SECRET=v}]]);_a80‍.w("../../models",[["refresh_token",["refresh_token"],function(v){refresh_token=v}],["User",["User"],function(v){User=v}]]);_a80‍.w("../../services/CustomerrorHandler",[["default",["CustomerrorHandler"],function(v){CustomerrorHandler=v}]]);_a80‍.w("../../services/JwtServices",[["default",["JwtServices"],function(v){JwtServices=v}]]);





const refreshController = {
    async resfresh(req, res, next) {
        // validate
        const registerSchema = Joi.object({
            refresh_token: Joi.string().required(),
        })

        const { error } = registerSchema.validate(req.body);

        if (error) {
            return next(error);
        }

        // database
        let refreshtoken
        let userId
        try {
            refreshtoken = await refresh_token.findOne({ token: req.body.refresh_token });
            if (!refreshtoken) {
                return next(CustomerrorHandler.unAuthorized('Invalid refresh token'));
            }

            try {
                const { _id, role } = await JwtServices.verify(refreshtoken.token, REFRESH_SECRET);
                userId = _id
            } catch (error) {
                return next(CustomerrorHandler.unAuthorized('Something went wrong'));
            }

            // check user in database
            try {
                const user = await User.findOne({ _id: userId });

                if (!user) {
                    return next(CustomerrorHandler.unAuthorized('No user found!'));
                }

                // generate new tokens
                const access_token = JwtServices.sign({ _id: user._id, role: user.role });
                const Refresh_token = JwtServices.sign({ _id: user._id, role: user.role }, '1y', REFRESH_SECRET);

                await refresh_token.create({ token: Refresh_token });

                res.json({access_token: access_token, refresh_token: Refresh_token});


            } catch (error) {
                return next(CustomerrorHandler.unAuthorized('Something Went Wrong'));
            }

        } catch (error) {
            return next(error);
        }

    }
}

_a80‍.d(refreshController);