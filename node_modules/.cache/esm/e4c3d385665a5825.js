let refresh_token,User,CustomerrorHandler,bcrypt,Joi,JwtServices,REFRESH_SECRET;_665‍.x([["default",()=>_665‍.o]]);_665‍.w("../../models",[["refresh_token",["refresh_token"],function(v){refresh_token=v}],["User",["User"],function(v){User=v}]]);_665‍.w("../../services/CustomerrorHandler",[["default",["CustomerrorHandler"],function(v){CustomerrorHandler=v}]]);_665‍.w("bcrypt",[["default",["bcrypt"],function(v){bcrypt=v}]]);_665‍.w("joi",[["default",["Joi"],function(v){Joi=v}]]);_665‍.w("../../services/JwtServices",[["default",["JwtServices"],function(v){JwtServices=v}]]);_665‍.w("../../config",[["REFRESH_SECRET",["REFRESH_SECRET"],function(v){REFRESH_SECRET=v}]]);






const loginController = {
    async login(req, res, next) {
        const loginSchema = Joi.object({
            email: Joi.string().email().required(),
            password: Joi.string().pattern(new RegExp('^[a-zA-Z0-9]{3,30}$'))
        });

        const { error } = loginSchema.validate(req.body);

        if (error) {
            return next(error);
        }

        try {
            const user = await User.findOne({ email: req.body.email });
            if (!user) {
                return next(CustomerrorHandler.wrongCrendentials());
            }
            // compare the password
            const match = await bcrypt.compare(req.body.password, user.password)
            if (!match) {
                return next(CustomerrorHandler.wrongCrendentials());
            }

            // generate jwt token
            const access_token = JwtServices.sign({ _id: user._id, role: user.role });
            const Refresh_token = JwtServices.sign({ _id: user._id, role: user.role }, '1y', REFRESH_SECRET);

            await refresh_token.create({ token: Refresh_token });

            res.json({ access_token: access_token, refresh_token: Refresh_token });
        }
        catch (err) {
            return next(err);
        }
    },

    async logout(req, res, next) {
        const logoutSchema = Joi.object({
            refresh_token: Joi.string().required(),
        });

        const { error } = logoutSchema.validate(req.body);

        if (error) {
            return next(error);
        }

        try {
            await refresh_token.deleteOne({token: req.body.refresh_token});
        } catch (error) {
            return next(new Error('Something went wrong in the database'));
        }

        res.json({status: 1});
    }
}

_665‍.d(loginController);